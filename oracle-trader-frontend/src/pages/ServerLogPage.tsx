// @deprecated - Use @features/logs instead
// COMPATIBILITY SHIM: This re-exports the moved component to maintain existing imports
export { ServerLogPage as default } from '@features/logs';

// Also provide named export for consistency
export { ServerLogPage } from '@features/logs';
