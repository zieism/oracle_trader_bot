// @deprecated - Use @features/dashboard instead
// COMPATIBILITY SHIM: This re-exports the moved component to maintain existing imports
export { DashboardPage as default } from '@features/dashboard';

// Also provide named export for consistency
export { DashboardPage } from '@features/dashboard';
