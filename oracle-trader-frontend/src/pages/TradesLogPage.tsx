// @deprecated - Use @features/trades instead
// COMPATIBILITY SHIM: This re-exports the moved component to maintain existing imports
export { TradesLogPage as default } from '@features/trades';

// Also provide named export for consistency
export { TradesLogPage } from '@features/trades';
